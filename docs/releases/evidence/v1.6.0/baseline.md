# v1.6.0 测试基准（进行中）

- **唯一客户输入**：[`esp32MiniRequire.md` 的「一、客户原始需求」](../../../../esp32MiniRequire.md#一客户原始需求)。按 UTF-8 从标题 `## 一、客户原始需求` 起，到第二节前连续两条分隔线前止，SHA256 为 `e62ebb05ecedb82e103d81b31da2bbf26aefe0669e580961c3aae87fef7d74d0`。执行 Agent 不接收本文件的验收表、预制 BOM、UUID、网表或目标坐标。
- **现场起点**：用户登录的 Web EasyEDA Pro 4.1.60。原专用项目 `easyeda-v160-schematic-test-20260923`（`cc931a5b30a0447e8886d6b62e975ccf`）P1 已部分写入，保留为失败样本。新专项项目 `easyeda-v160-schematic-clean-20260924`（`4037dd22434c412fa75d7f2d855a2ab4`）创建时三页均经完整回读证实空白。随后仅 P3（`a6db7873157483f0`）已按参数源写入 CH340C 模块；P1/P2 仍未写入设计对象。新旧工程不混算。
- **待测运行时**：P3 最新现场读取使用本地 CLI、daemon、Skill、连接器精确 `1.6.0-dev.13`，候选提交 `00582f1`；`dev.14` 求解器仅离线重算，尚未安装到 Web 窗口或验收现场。以每轮 `pcbpilot health` 的实际运行版本及目标工程/文档为准。连接器开发热更新核对旧/新 bundle 哈希后重载。正式发布候选仍须重新固定版本及哈希，不能沿用开发版结果。
- **版本化源与事实**：所有布局以原始官方快照、源数据、每件实测位号几何、参数和候选哈希重算；历史 `27+4` 件只是旧样本观察值，不是新执行的固定件数。清页前完整图元状态守卫。属性可见性 getter 返回 `undefined` 时，只有官方当前工程源的目标页同 ID/key/parent/value 属性明确给出 `boolean/null`，才允许恢复该字段；源缺失或不符仍拒绝，不猜默认值。宿主自动生成且 SDK 已完整可读的属性不要求存在于工程源。每页采用新鲜快照，写入后显式保存、真实重载、新鲜对象回读和 DRC。截图只辅助发现问题。
- **通过阈值**：客户要求逐条映射，所有物理脚有可信 pin→net/NC；核心及专属外围整体移动，范围外对象不变；位号/导线/框/图签无碰撞；逐页 strict gate 通过、0 overlap、0 fatal、网络连通，保存重载后保持；完整固定端到端用例按 S0–S6 + P0–P10 验收 4 层电源树、丝印极性、天线 keepout、已落盘。原理图专项完成只算专项通过，不能代替整板通过。
- **当前结论**：基准已固定。P3 参数源已现场写入并在保存重载后完整回读，官方导出图可见；其严格验收仍有 6 条平台汇总警告和图签越界显示。P1/P2 区内求解仍以有界预算停止，完整三页和整板现场测试未完成，不得生成 `pass` 发布清单。

详细失败与证据见[当日现场记录](../../../reviews/2026-09-23-v1.6.0-schematic-acceptance.md)。
