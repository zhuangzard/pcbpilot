# v1.6.0 测试报告（进行中，未通过）

**总体结论：未通过发布验收。** 本地 `1.6.0-dev.4` 的离线重算和清页守卫复核通过；
三页现场完整 Apply、严格门禁、保存重载、局部修改及整板端到端仍未完成。
本文件只记录当前事实，不能作为正式 `v1.6.0` 的通过报告。

`dev.4` 写前 P1 完整图元读取报属性 `KeyVisible` 不可读，组合请求还经历重连并在
150 秒超时。`dev.5` 加入按属性 ID 复读与断线快速失败。用户刷新测试标签后，
CLI/daemon/connector 稳定精确 `dev.5`；P2/P3 的完整图元读取通过，P1 属性
`a49d99a710e26ebb` 经按 ID 复读仍返回不受支持的 `undefined`，写前保护拒绝。
`dev.6` 经有界热更新后，三种官方属性读取路径仍都返回 `undefined`，文档身份未漂移。
为继续空白起点测试而尝试新建专用工程时，显式传 team 的 `project.create` 返回空 UUID；
`dev.7` 只读完整核对目标团队根目录 50 个工程，无同名项目。省略显式 team 后创建成功；
新项目三页完整空白快照已通过。无历史上下文 Agent 从原始需求独立建立 32 件源，
专用测量页的官方位号/引脚几何与清理后 save→reload→fresh 回读已保留证据。
P1 的 7 件 USB 单区和 P2 的 13 件 MCU 单区均以 20000 候选复现预算耗尽；
没有生成或执行正式 Apply 队列。详见下方现场记录。
后续 `dev.8`/`dev.9` 的通用搜索修复通过离线抽象正反测试，但 `dev.9` 对冻结的
P1/P2 完整预算输入仍分别耗尽 200000/100000 候选；这些版本未安装到 Web 窗口，
不改变本报告的现场结论。
`dev.14` 候选补了命名出口预检、路由候选的完整线岛命名约束、共享预算和失败归因回归；
全量 Go 测试 3622 项通过。对原始字节 SHA-256 分别为
`9b8ae6e81044ca407d0818d189ca486fbf53fb2d08965650aedbdba8bd7dd0c6`、
`3430fa10a371f22410ab64afeb1867793bf73985d25acc6ae7b00cc2faacb775`
的冻结 P1/P2 源重新运行后，仍各耗尽 200000/100000 候选；报告为
`P1-layout-report-dev14.json`、`P2-layout-report-dev14.json`，其 `sourceSha256`
与输入原始字节一致，`globalInfeasibilityProven:false`，且没有输出完整布局。
这只能说明当前有界搜索未找到解，不能把两页判为几何不可解或用于现场 Apply。
按两区拆分建议另存 P1 USB 与 P2 MCU 的离线源副本并运行 `sch zone-review`：
分别因跨区 `USB_DN_CONN`、`MCU_EN` 仍声明 `direct` 而写前拒绝。
两区现有专属附件分别连成覆盖全部 7 件、13 件的依赖树；改成同名端口会丢失
必需的真实直连，单独剥离外围则破坏归属。因此未将这些副本交给求解器或现场 Apply，
这两份拆分副本未改变冻结输入或当时的正式页面，总体未通过结论不变。
详见专项记录中的哈希和复核。

### 2026-09-24：P3 现场阶段结果（`dev.10` 写入，`dev.13` 回读）

在专用工程 P3 `a6db7873157483f0`，参数源生成的 37 步受保护队列执行了前 35 步：
U3 CH340C、C8、10 个网络标记、导线和模块框均写入，逐脚检查与线树检查通过。
第 36 步严格门禁因图签字段和 6 条平台 DRC 汇总警告失败；未跳过旧队列续跑。
随后以 typed `sch titleblock` 填充字段、`sch save`、`doc reload`。`dev.11` 的
完整对象回读被宿主属性可见性 `undefined` 阻塞；官方当前工程导出确认目标属性原生值为
`null`。`dev.12` 又发现 SDK 有一条可读的自动生成属性不在工程源中；`dev.13` 仅对真正
不可读属性按同页、同 ID/key/parent/value 恢复，完整回读成功。证据保存在本地忽略目录
`artifacts/easyeda-v160-schematic-clean-20260924/`：
`P3-postreload-full-list-dev13.json` 精确返回目标工程/页、13 个元件类对象（含图纸和标记）、
10 条导线图元、267 条属性、1 矩形、1 文本；官方
`P3-live-after-reload-dev13.png` 为保存重载后的全页图。三份本地证据的 SHA-256 分别为
参数源 `86706dfd51ba6cf2c23ad7f73382b25351b5ac1d853b3ec483de137d52fb7e33`、
完整回读 `b370ce7dd588ba8c05e868995b65869bc95ab5e9a8ae94546d9e87fb6e1da44b`、
导出图 `35450fe87a5bb007e8378696af2b574f6a4cd6b415766b1154b16963553f17f0`。

复测 `sch check` 为 0 浮脚、0 网络/标记不符、0 多网导线、0 图签碰撞、3 条信息级无接点交叉；
`bridge-check` 为 0 桥接/孤儿；`clusters --strict` 为 2 组、0 重叠、0 出纸；
`layout-lint` 为 2 件、0 紧贴、0 出纸；官方 `sch drc` 为 0 fatal、0 error、
**6 warn 且不提供逐项详情**。导出图还显示 `Name`/`Description` 被宿主绘在纸框外。
图签修正后重新运行 `dev.13` 的 `sch gate --strict --json`，结果仍为 `fail`，
唯一 blocker 是这 6 条未归因的 DRC 警告；其余阶段没有 blocker。
因此 P3 是**已落盘的局部阶段结果**，严格 F2 和发布验收仍未通过。
无历史上下文的独立 Agent 仅用冻结证据复核：P3 原生工程源与 `dev.13` 完整回读的
13 COMPONENT、10 WIRE、1 RECT、1 TEXT 原语 ID 集合一致；U3/C8 共 18 个物理脚的
11 条 pin→net、7 个 NC 与参数源逐项一致。其结论只支持 P3 局部落盘与回读，明确不签
F2 strict gate、三页完成或整板验收；图签越界和 6 条无逐项详情的 DRC 警告仍是缺口。

| 用例 | 当前状态 | 可核事实 / 下一道门槛 |
|---|---|---|
| V1 | `dev.13` 现场部分通过，正式版待测 | 本地 CLI/daemon/connector 精确同版，P3 保存重载后完整对象回读成功；正式发布候选及三页连续切换仍待测。 |
| V2 | `dev.7` 专项现场通过，正式版待测 | 带 team 创建返回空 UUID；只读根目录清单完整读取 50/50 且同名缺席；省略显式 team 后创建并打开唯一新工程，三页身份与空白快照对账。 |
| M1 | `dev.7` 专项通过，独立离线复核通过 | 32/32 唯一可见官方位号 bbox、属性 ID/parent 和全部 pin 几何一致；专用测量页 typed 删除后，三张正式页 save→reload→fresh 完整对象与初始快照精确相等。 |
| M2 | 部分通过 | 三页官方 SVG 与 fresh 图纸对象已配对，红色 A4 内框 raw `[10,10,1160,815]`、图签/Logo 保守禁放区 `[454,1,1166,196]` 用于 P3 写前；P1/P2 全页候选和保存后对象/图像的完整入框仍未复核。P3 图签附加字段可见越界。 |
| F1 | 源一致性离线复核通过，完整用例阻塞 | 新源为 32 件、139 物理脚、23 网、99 条 pin→net 与 40 个明确 NC，逐脚无重叠/遗漏，归属与附着检查通过。`dev.14` 对冻结 P1/P2 仍分别用尽 200000/100000 候选，`globalInfeasibilityProven:false`；两区拆分副本因跨区 `direct` 写前拒绝，独立复核还发现跨区专属附件；P3 区内求解成功。未产生三页完整真实 direct 导线树、整页计划和 guarded 队列。 |
| F2 | P3 局部落盘，整体未通过 | P3 前 35 步写入后，第 36 步严格门禁拦下；单独 typed 保存、重载、`dev.13` 完整对象回读与导出成功。图签修正后的新鲜 strict gate 仍仅因 6 条未归因 DRC 警告失败，图签附加字段也可见越界。P1/P2 未 Apply，三页 strict gate 不成立。 |
| E1 | `offline-only`，现场未测 | 独立复核可沿源 netlist 追溯 USB 供电/数据→CH340C→UART 与 DTR#/RTS#→EN/BOOT；理想 NPN 静态四态 LL→HH、LH→HL、HL→LH、HH→HH。尚无完整实际导线和现场回读，不能判现场通过或实板可烧录。 |
| L1 | 未运行 | 尚无经保存重载确认的完整基线。 |
| L2 | 条件未满足 | 未发现可归因且可回读的现场旧支路错误。 |
| N1 | 离线通过，现场待测 | `dev.4` 完整清页守卫及零删除负例经独立只读复核；目标宿主 SDK 能否完整读取仍待验。 |
| R1 | 离线部分通过 | 八区、三页、compose 重算一致；现场中断恢复与保存后幂等未测。 |
| E2E | 未运行 | PCB S0–P10 的布局、布线、DRC 与持久化不在当前专项结果内。 |

现场测试工程为 `easyeda-v160-schematic-test-20260923`。原始失败、执行 prompt、版本、
项目/文档 UUID、逐项事实及证据索引见
[2026-09-23 原理图专项记录](../../../reviews/2026-09-23-v1.6.0-schematic-acceptance.md)。
本地忽略目录 `artifacts/easyeda-v160-schematic-20260923/` 保留原始 CLI 回包与冻结哈希；
它不是可公开发布的附件。后续须在刷新后的运行态重新编队列、完成现场验证，交无历史
上下文 Agent 独立复核；仅在全部必跑用例通过后改写本报告和 `manifest.json` 的结论。
