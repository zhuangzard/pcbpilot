# ADR 0003: 原理图布局 = 同一个刚体求解器递归三层(part→group→zone→sheet)

## Status

Superseded for the schematic generation workflow (2026-09-14)。现行主链见
[数据驱动架构基准](../../.agents/skills/pcbpilot/references/schematic-data.md#数据驱动架构基准)。
下文 Accepted 是历史决策记录；`solveLayer` 三次递归不再定义新设计唯一入口。
保持完整区域、纯计算和数据校验的原则沿用，生成由区内与纸张两层承担。

Accepted. (2026-08-14) — 用户拍板。取代此前「块布局 / 区内重排 / 整页分区」
三套各自为政的实现。

## Context

到 2026-08 为止,原理图自动摆放是**三批瀑布**,每批只能看见前一批的残局:

```
block-apply   → 器件坐标(关系求解器 bslSolveAround)
     ↓ 器件固定,不可回头
autoconnect   → marker 逐个 pin 独立贪心挑 direction×offset
     ↓ 器件 + marker 都固定
zone-draw / note → 区框、注释,**事后独立命令**,捡剩下的空隙
```

症状是**打地鼠**:同一类病(间距、碰撞、对齐、判定口径)在三处各犯一遍,修好一处
另两处不同步。最近一次实证:marker 重叠的根因是「评分器与 `sch check` 用两把尺」
(c43b83e,真机 12→3),而那只是**一层内部**的两把尺;层与层之间还有三把。

用户对此的判断:「你现在是用根本的方案设计的 sch 功能吗?还是只是在调整?」

## Decision

### 1. 运行时只有四层,block 不是层次

```
part  →  group(虚拟组)  →  zone(功能区)  →  sheet(页)
```

**`block` 是「虚拟组的配方」,不是运行时层次**:一份「哪些 part + 它们之间什么
关系」的声明。归组之后它就是个普通虚拟组,上层不需要知道它来自哪个块,也不该有
任何「块专用」的布局路径。

> 用户原话:「我们应该不要过多的理解这个块的概念,blocks 的概念,还是应该拆散了,
> 它其实都是 part 元素构成的。」

### 2. 三层是同一个算法的三次递归

三步的输入输出**完全同构** —— 一组刚体 + 它们之间的关系 + 一个边界 → 每个刚体的位置:

| 层 | items(刚体) | 关系来源 | 边界 |
|---|---|---|---|
| 1 | part | 块配方的 flow/attach/pair + 实测引脚 | 组的可用区 |
| 2 | group | **跨组网数量 + 端口 dir 推导**(见 4) | 区的可用区 |
| 3 | zone | 跨区网 + 页面惯例 | 图纸可用区(减图签) |

因此收敛成**一个函数,调用三次**:

```go
// solveLayer 是全部三层唯一的布局入口。层与层只换 items / rels / bounds。
func solveLayer(items []rigid, rels relations, bounds layoutBBox) []placement
```

**这是本 ADR 的核心决策**。三套代码(`bslSolveAround` / `zone tidy` / `zone-plan`)
各自的间距常量、碰撞判定、对齐逻辑必须收敛到这一个实现;不收敛就还会继续打地鼠。

### 2.5 「组」的边界由**导线连通性**定义,不是由块成员定义(2026-08-15 用户拍板)

上面第 1 节写的四层里,`group` 这一层此前实际是「块实例的成员集合」。用户看图后指出这不对,
并给了定义(全文见 [`docs/concepts.md`](../concepts.md#原理图侧的刚体虚拟组-l1-与功能电路-l2--用户定义2026-08-15)):

- **L1 虚拟组 = 导线连通分量**(件 + 桩线 + marker + 文字);**只靠网络标签同名相连的不算**;
- **L2 功能电路** = 若干 L1 组按功能聚合(块实例天然是一个 L2);
- **两层同一条铁律:组的体积 = 全部元素的并集,同层组之间不许重叠。**

这个定义是对的,因为**导线连通分量正是「挪一个必须一起挪」的单位** —— 拆开它就要删线重连
(踩「相邻共线导线合并 → 串网」);而 L2 之间只有标签关系,整体平移不动电气。所以
L1 是刚体的物理边界,L2 是语义边界。

实测(ceshi,单块 ch340c):L1 组 **6 个**(J1 与 D1 靠 D+/D− 真实导线直连,是同一个组),
**2 对整块重叠**:`U3 ↔ C7 (71×85)`、`U3 ↔ C8 (21×81)`;另有 `J1+D1` 组左沿 x=**−26**,
探出图纸。而 `layout-lint` 默认排除 35 个非 part 图元,一路报 `0 overlap` —— **判据先瞎了**,
这是「判定与生成同一把尺」在组这一层的显形。

**重叠的机制,以及由此拍板的 A 方案**:U3 左侧 6 个 marker 要 276 深的通道,而 C7/C8 是贴在
U3 电源脚上的去耦,就坐在那条通道里。根子是一对天然冲突 —— **去耦要贴脚,marker 要从同一侧
引出**。三种解法里选 **A:去耦改贴宿主的上/下侧**(marker 保持左右引出)。理由:去耦贴上下
不损失电气质量(离脚一样近),而让 marker 换侧会撞上「背面引出是红线」那条已经定死的判据
(fa8f969),让 marker 绕更深则是把版面赔进去。

因此 `bslAttachSide` 的旧推导(「引脚在左/右列 → 该件沿 x 推出去,方向正交天然不撞」)**是错的**:
它只比了「该件自己的 marker」与「宿主该脚的 marker」,没算**同侧其它引脚的 marker 会横扫过去**。

### 3. 组内相对位置在归组时固化;上层只平移,不重塑

一旦成组,组内相对位置**不再改变**,组对上层就是**刚体**。上层对下层只有一个动作:
**整体平移(+ 必要时整体旋转)**。

**明确否决「软模块」**(为装箱而改变组的长宽比)。理由:VLSI floorplanning 里软模块
成立是因为那是硅面积、形状无所谓;**原理图的组内画法是可读性惯例**(CH340C 就该
USB 在左、串口在右、去耦贴 VCC),为塞进空档把它重排成竖条 = 用可读性换密度,方向
是反的。**组内相对位置是电路语义,不是布局的自由度。**

放不下的正解是**翻页**,不是重塑:功能拆分到其他页正是多页原理图的意义。

### 4. 组间关系可以算,不需要新声明

组与组之间不能只有碰撞(纯碰撞 = 不重叠、填得满、读不懂)。但顺序信息**已经在
数据里**,不必让块作者再声明:

- 两组之间的**跨组网条数** → 决定该多近(强连接的靠拢)
- 端口的 **`dir`(in/out/bidir)** → 决定左右顺序(信号流向)

同理 zone 层。

### 5. marker 在归组前挂上,组的 bbox 天然含 marker

第一步的次序必须是:**放件 → 连线 → 挂 marker → 封组**。

于是组的 bbox **本身就包含** marker 符号、桩线和文字带,上层拿到的刚体尺寸就是
真实占地。**因此不需要单独的 courtyard/通道预留概念** —— 不是估算通道深度,是
放好之后直接量。(曾提议给器件算「四侧引出通道」,按本设计不必要,已弃。)

### 6. 每一层都要独立做一遍边界收拢,「坐标」永远不能凌驾于「不出界」

`solveLayer` 的 `bounds` 参数不是装饰:**每一层都必须保证自己的 items 落在自己的
bounds 内**,不能指望上层或下层代劳(元素→组用组的可用区,组→区用区的可用区,
区→页用图纸可用区减图签)。

**出界与重叠不同等对待**:

| 情况 | 处置 | 理由 |
|---|---|---|
| 与已有内容**重叠** | 尊重显式坐标,警告 | 图还能读,人能事后挪 |
| **超出可用区** | **自动收拢到最近合法位置** | 落在图纸外的器件在导出/打印/评审里根本不存在 = 废图 |

坐标是**偏好**,不出界是**硬约束**,偏好不该覆盖约束。旧行为是「按你的坐标照常放置
(显式 --at 优先)」+ 一条 warning,于是每次给一个越界的 `--at`,块就真被放到纸外面。
(已修:`bapResolveOrigin` 的 `AtExplicit` 分支,真机 `--at 700,600 → 660,600`,
`layout-lint --strict` 0 out-of-sheet。)

### 7. 「填满纸张」不是目标

功能区之间的留白是**可读性的组成部分**。标准是「区内紧凑、区间有隔」,不是密度
最大化。

## 落地机制:place 与 connect_pin 之间的时间窗

(2026-08-14 补,来自一次十路 agent 的代码侦察 + 对抗评审。)

「marker 挤死时把压力反推成器件位置调整」曾被认为与「无 undo」冲突。**不冲突** ——
存在一个时间窗让挪件零风险:

```
place 全部件  ─────────┬─────────  connect_pin
                      ↑
        器件已落地:有实测 bbox、最终位号、最终网名
        marker 一根都还没建:没有桩线可被合并,挪件 = 一次 component.modify(实测 10–23ms)
```

**这个窗口是整套反馈机制成立的唯一原因**。窗口之外挪件都要面对「删桩线 → EasyEDA
合并相邻共线导线 → 当场串网」(已三次真机失败,`sch destagger --apply` 因此禁用)。

**推让必须是链,不是一件**(2026-08-15 实证):第一版只推「该侧最近的那一件」,真机把
D1 往左推开 146,正好把它按在了 J1 身上(真实 bbox 边到边只剩 **12**,低于自己的
`bslPartGap=20`)—— 通道是从另一处的挤压里抢来的,不算腾出来。现在推让是**一维约束求解**:
① 通道带里的件差多少让多少 → ② 内侧件让开后,外侧件跟着让「位移 − 自己那段富余」→
③ 每一级自己的上限(可用区 / 推不动的外部图元)**反推回内侧**,链上任何一处顶住就**整条链
一起截短**。宁可少推也绝不推一半 —— 半推等于把内侧件按在外侧件身上,那是 part×part 重叠
(layout-lint 的 ERROR),比 marker 挤在一起更严重。pair 组整体平移(等距是电路语义,
见 §3),attach 件既不推**也不当墙**(它占的是自己那条脚的 lane)。
真机(单块 ch340c):D1↔J1 真实间隙 **12 → 57**,U3 左通道 **238 → 259**,
markerOverlaps 三轮 8/8/4 → 6/6/5。

**推让解两遍:落地前用估算,落地后用实测**(2026-08-15 起)。估算与真值差得不小 ——
`tvs.` 估半宽 10,而 D1 实测 bbox 是 `[358,406]`:**锚点根本不在 bbox 中心**,右侧伸出 36;
`conn.` 估 90 而 J1 实测只有 35。于是估算那一遍以为通道 275、实际只有 259。所以:

- **落地前**(`bslExpandForMarkers`)用 `bslPartBox` 估算 —— 它决定件**创建**在哪,
  把大头一次性让到位,省掉一堆 modify;
- **落地后、布线前硬门之前**(`bslExpandLive`)用**实测 bbox** 再解一次,差多少补多少。
  `bslPushSolve` 是纯几何函数,换一批 box 进去即可,求解器本体一行没改。

顺序是硬约束,不是偏好:**推让必须排在带引脚的硬门回读之前**(见下条),而硬门因此验的是
推让之后的最终几何 —— 只验一遍,且验的是最终态。硬门若在推让后报重叠,先把位移原样还原
再验一次(坐标是自己写进去的,还原是精确的):版面优化失败不该让整单回滚。

真机(单块 ch340c):U3 左通道 **259 → 274**(目标 276,差的 2 是落格下取整),
D1↔J1 仍有 42(> `bslPartGap`),三轮完全一致。

**平台坑:带引脚的回读会毒化紧随其后的 `component.modify`**(2026-08-15 实测确定复现)。
`components.list --include-bbox --include-pins` → `modify`,8 轮 4 败,失败在 1–7ms 内,
errorDetail 是 `Cannot destructure property 'cmdKey' of 'i' as it is undefined`;换成
`--include-bbox`(不带引脚)→ 10 轮全过;完全不读连发 10 次 modify 也全过。根因在连接器的
引脚分支会调 `sch_ManufactureData.getNetlistFile()` —— **读引脚顺带跑了一次 netlist 导出**。
存盘、重试、等 500ms 都救不回来,**唯一正解是排序**:要挪件就别在它前面读引脚。
同一根因还咬掉了 `sch align/distribute --apply`(它读几何时带 `includePins`,却一个引脚
字段都不用,随后立刻 modify)—— 已一并去掉。**下次再看到 modify 被无理由拒,先查它前面
有没有带引脚的回读。**

**新的瓶颈在 marker 落点,不在版面空间**:通道给足之后 markerOverlaps 仍是 6,而这 6 处
里 4 处是 1 个单位的边沿相碰(`26×1`、`31×1`、`1×11`)。这不是"没地方放",是
**lane 步长与实际渲染的标签尺寸对不上** —— 「判定与生成同一把尺」在 marker 这一层的又一次
显形,下一步该查 `laneStepFor` / `predictedMarkerBBox` 与 `sch check` 量到的真实标签带。

配套事实(侦察实证):

- **批1→批2 之间只有 3 个字符串过河**:block-apply 把整个块计划压扁成
  `[]acConnSpec{PinRef,Kind,Net}` 交给 autoconnect(`cmd_sch_block_apply_run.go:773-784`)。
  几何、role、块身份、关系**全部丢弃**,marker 规划器只能重新从画布猜「C8 是贴在
  U1:VCC 上的去耦」。所以边界不是「没数据」,是**数据被丢弃后重读**:同一份场景在
  一轮 apply 里被读了 4 次(`:101`、`bslResolveLive`、`verifyBlockLayout`、autoconnect 自己)。
- **重叠的性质**:实测最密真页 68 处重叠**全部是 marker×marker,part×part 为 0**,
  且 marker 到最近器件的距离中位数恰好 30(= `schStubLen`)——所有人挤在同一条桩长上。
  **同侧 lane 错列**(lane 间距按该侧最宽 marker 的实宽,netport 用 `relayoutPortWidth`)
  取代「每 pin 独立贪心 44–148 候选」,是有实测支撑的解法。
- **注释/区框读不回来**:文本走 `schematic.text.list` 且平台**不给 bbox**(只能按字数估);
  矩形框**没有任何枚举 action**,只有 primitiveId 记在磁盘状态里(无几何)。所以
  「注释/区框同级参与碰撞」**不能靠读回,只能由求解器自己持有几何**。
- **唯一已存在的「位置与 marker 一起决定」的管线**是 `sch zone relayout --apply`
  (`cmd_sch_zone_relayout.go` → `tidyApply`):删净成员桩线/旗 → modify 落位 →
  `connect_pin` 一遍性重连 → 自检失败逐步回滚。统一求解器的落地管线**复用它,不另造**。
- **同一个部件今天有三套尺寸**(保守半宽 `bapRoleHalfExtent` / 贴脚紧凑半宽 `tightHalf`
  / 实测 bbox)——「判定与生成同一把尺」这条定律的**第三次显形**。

## Consequences

**断链已接上(58c1da1,真机验过)**:`block-apply` 完成后**不登记虚拟组**,第一步的
产物是一堆散件,第二步 `zone tidy` 手里没有刚体可排。现已在 apply 收尾处登记持久
虚拟组(`grouped ✓ g1 (ch340c_usb_serial(C7)) — 7 件`),fail-soft。顺带修出一个真
bug:`sch clear` 删光图元却不清组表,留下指向已不存在器件的孤儿组,下一次 apply 想
登记同名位号会被自己上一轮的残留挡住。

**保留复用**:关系语义(`flow`/`attach`/`pair`)、实测引脚驱动的两阶段、
`bslReach`/`bslFlowGap` 这类**从数据算间距**的函数、marker 落点评分(含
`predictedMarkerBBox` = 本体 ∪ 文字带的同尺口径)、`layout-lint` / `sch check` 的
真实 bbox 判定门。

**要收敛(不是删除,是并入 `solveLayer`)**:`bslSolveAround`、`zone tidy`、
`zone-plan` 三处各自的间距/碰撞/对齐逻辑。

**沿用的既有定律**(违反即失败,见 [`docs/concepts.md`](../concepts.md) 与仓库记忆):

- 判定与生成必须**同一把尺**(已两次显形:块布局的半宽、marker 的文字带)
- 避让**只沿关系自己的轴**推,另一轴钉死(环形推让会当场破坏 flow 共线 / pair 成对)
- `#64` 硬拒绝(桩线触异网导线 = 静默短路)永不可降级为软惩罚
- 无程序化 undo;SDK 往返贵(关系求解只回读一次锚件)
- y-UP;确定性(同输入同输出,不得依赖随机/时间)

**验收判据(可量化,真机)**:每一期都要能独立验证 —— `sch check` 的
marker-overlap 条数、`sch layout-lint --strict` 的 overlap / out-of-sheet 数、
SDK 往返次数。不接受「看起来更好」。固定用例仍是
[`esp32MiniRequire.md`](../../esp32MiniRequire.md)。
