# 1.6.0 本地候选：原理图参数化验收

## 目标与边界

验证本地 `1.6.0-dev.1` 首轮及修正后的 `1.6.0-dev.3` 候选在 Web EasyEDA Pro 中能从原始需求生成完整原理图，
并从保留的源数据做有范围的局部修改。原理图专项止于 S6；PCB、正式 `v1.6.0` 发布
和旧工程的历史通过记录均不计入本轮结论。操作和判据以
[数据驱动架构基准](../../.agents/skills/pcbpilot/references/schematic-data.md#数据驱动架构基准)、
[执行流程](../../.agents/skills/pcbpilot/references/auto-layout-sop.md)及
[现行验收标准](../e2e-automation-acceptance.md)为准。

测试工程按用户要求新建为 `easyeda-v160-schematic-test-20260923`，不改动既有 `ceshi`。
输入只给
[`esp32MiniRequire.md`「一、客户原始需求」](../../esp32MiniRequire.md#一客户原始需求)；
不给 BOM、UUID、网表、器件坐标、预制源 JSON 或答案图。测试前确认工程/文档在
`pcbpilot health.windows` 中精确出现，并记录 CLI、daemon、Skill、connector 与宿主版本。
同一窗口所有 typed 调用串行执行。

## 给无历史上下文 Codex 的执行 prompt

执行与独立评审均新建 `fork_turns: none` 的 subagent；只传下述任务和指定原始材料，
不要继承本报告或前一执行员的判断。

> 你是独立测试执行员。只读取本仓库 AGENTS.md、公开 easyeda-agent Skill，以及
> `esp32MiniRequire.md` 的「一、客户原始需求」作为客户输入。在已连接的 Web EasyEDA Pro
> 测试工程 `easyeda-v160-schematic-test-20260923` 中，自行选型、建立真实连接，声明每个功能核心及其专属外围、约束和
> 纸张策略，再走原始快照 → 源数据 → 区内布局 → 整页布局 → 固定转换 → 受保护 Apply。
> 布局前逐件测得唯一可见位号的官方 bbox 并保留 parent/页身份；图签字段写进逐页源，
> 位号、导线、内框和图签都参与候选校验，缺测不得算布局通过。
> 先将客户每条需求映射到源对象和可观察的验收事实，标清无法由原理图证明的功能。
> 对“插上 USB 就能烧录固件 / 看串口”按免手动按键的严格含义核查控制路径；
> 如有不同解释，先记录假设，不能仅凭 USB 数据线和 BOOT 按键宣称满足。
> 只验原理图至 S6。保留每阶段输入/输出和哈希；对实际对象独立回读、检查、显式保存，
> 真实重载后再回读。随后基于同一保留源，选择一个有合法空位的核心做局部移动；只有现场
> 存在可回读旧线和旧标记的真实单脚问题时才运行标记修复。逐对象报告目标范围、范围外
> 不变量、未覆盖项与原始失败证据。所有工程写入走 typed CLI；若连接、加载、保存或回读
> 失败，停止现场写入，不用 GUI、任意 JS 或截图补缺失证据。不接受历史报告或队列自报
> 为本轮现场验收结论。

评审者不得把本报告的场景、验收矩阵或历史工程数据传给执行员作为客户答案；仅传上面的
流程 prompt 和原始需求。独立评审再读实际证据，不能沿用执行员的自评 verdict。

## 场景与判据

| ID | 输入或触发 | 必须观察到的事实 |
|---|---|---|
| M1 位号测量 | 每页所有实际 part 或专用测量页 | `sch designator-geometry` 逐件返回唯一可见位号、parent、真实 bbox、页身份和来源；件数与源相等，缺失/重名/隐藏/页漂移均非零失败，非位号属性不作障碍。 |
| F1 整体生成 | 原始需求；测试工程初始快照 | 每个器件有唯一核心/外围归属；选型、全部物理脚、pin→net/NC 与需求逐项对应；direct 保留真实线树；完整区内与纸张候选通过，固定渲染与 compose 使用同一几何，Apply journal 完整。 |
| F2 落图与持久化 | F1 的完整队列 | 实际组件/网络/导线/标记/框/位号/几何与目标逐项对账；逐页 `sch gate --strict` 为 pass；`sch save` 返回 `saved:true`；真实 `doc reload` 后新鲜回读仍一致。导图只辅助找漏项。 |
| E1 USB 烧录控制 | 客户“插上 USB 就能烧录固件 / 看串口” | 按免手按键的严格解释，确认 USB 转串口的数据通路及 DTR/RTS 到 EN/BOOT 的实际电气路径；四种控制线状态不得把双低误变成持续复位，BOOT/RESET 手动按键保留。原理图只能证明连线和静态逻辑，真正烧录与串口行为留待实板验收。 |
| L1 核心局部移动 | 保留源 + 新鲜页快照 + 测得的合法 5 raw 目标 | `sch layout-edit --move-core` 重新生成本区；核心和专属外围及附着图元一起移动，其他区组件身份、参数、网络、NC 和几何逐项不变；现场保存重载后的范围内外差分仍成立。另用受阻目标证实写前拒绝，不输出成功队列。 |
| L2 单脚标记修复 | 新鲜回读中确有可归因的旧引线、旧标记与错误 | `--repair-pin` 仅改变指定支路，沿真实引脚外向；问题消失，旧有 findings 与范围外对象不增加或不变。缺少真实前置缺陷则记 `not-run`，不造现场错误凑覆盖。 |
| N1 写前拒绝 | 过期快照、同数量但页身份漂移、跨页源数量漂移、非 5 raw 网格、缺归属、同一外围重复 attachment、direct 被同名标签代替、实例身份/属性缺测，以及清页前同器件集合但导线或网络标记已变等独立夹具 | 各类问题按对象/原因非零失败，尤其整页替换须在首次删除前拒绝任何未纳入快照的图元或状态；失败的离线阶段不产出可 Apply 的完整计划；不得把 `blocked` 或预算耗尽写成设计不可能。 |
| R1 重现与幂等 | 相同源、测量、参数和代码版本重复离线计算；局部写中断 | 哈希和目标几何一致；新鲜回读下重复转换不引入无谓重建；部分写/超时先读实际状态再重算，不盲重放。 |

## 证据与判定

每轮在仓库外或忽略目录保留：原始需求、未改写的官方快照、目标源、测量来源、算法参数
和提交号、SHA-256、完整候选/选中页、固定导图、队列 dry-run、执行 journal、fresh
`sch list`/`connectivity`/`read`、逐页 `layout-lint`/`check`/`bridge-check`/DRC/strict gate、
实例身份与 `uniqueId`/属性、范围外差分，以及 save/reload 后第二份 fresh 回读。
每轮冻结自己的版本、输入哈希和证据清单；逐条客户需求须标 `pass`、`fail` 或未能由
原理图证明。对象比对未覆盖图形时，不得用引脚连接一致代替完整原理图对账。
验收报告按场景记录 `pass`、`fail`、`blocked` 或 `not-run`，并给出证据路径和第一条失败对象。
缺测、连接器未加载、宿主持续加载、保存/重载失败一律不能记 `pass`。

## 本轮运行记录

- 构建源码起点：`dev@834fa7d` 加本轮版本元数据与测试计划变更；候选包版本：
  `1.6.0-dev.1`（本地，非正式发布）。
- Web 宿主：内置浏览器 EasyEDA Pro `4.1.60`。
- 初次 `pcbpilot health`：旧 git-describe 构建，`windows: []`。随后构建并安装候选包，
  停用市场版连接器，导入本地包；用户确认后启用外部交互权限。
- 2026-09-23 10:44 UTC：`health` 显示 CLI/daemon/connector 精确同版 `1.6.0-dev.1`，
  新建工程 UUID `cc931a5b30a0447e8886d6b62e975ccf`，原理图 P1 UUID
  `dff64dae759aedab`，均已在同一窗口回读。构建时 `make skill-check` 通过，
  本地包 `release-check.py --local-dev` 通过。
- 首轮盲审发现 `sch check` 的位号与导线相交算法把 bbox 当开区间，与公开基准的闭合
  障碍要求冲突；`dev.3` 已修正边界重合、端点触边和角点相切的判据，并补正反例。
  `dev.3` 还增加只读 typed `sch designator-geometry`，可在布局前导出逐件位号实测。
  Go/connector 测试、typecheck、Skill check、五平台本地构建与离线 smoke 已通过；
  代码本地提交为 `b032eeb`。这些不是现场通过证据。当前 Web 页尚运行旧连接器，
  等待用户按仓库规则刷新后复测。
- 图签缺口已通过逐页 `titleBlock` composition 源字段和受保护队列修复，本地提交
  `f0c6535`。字段写入后由 typed CLI 新鲜回读，并要求图框仍在；Go 全量 3553 项、
  `make skill-check` 与 `git diff --check` 通过。已重建并安装同版 `dev.3` CLI/Skill、
  重启 daemon，浏览器页仍须加载已安装的 `dev.3` connector。图签修复尚无现场结论。
- 同版 CLI 已把 P1/P2 的旧目标源加入逐页 `titleBlock`，P1 重新 compose 与受保护
  dry-run 成功（`79`–`81`）；这是旧几何的**离线编译检查**，不是最终布局或现场写入。
  最终源须在 27 件位号全部实测后重算。首轮成本画像已记录（`75`）：墙钟
  34.16 分、daemon 6.28 分，token 数未可靠记录。
- 独立执行证据保存在仓库忽略目录
  [`artifacts/easyeda-v160-schematic-20260923/`](../../artifacts/easyeda-v160-schematic-20260923/)；
  首轮失败与诊断见 `49-failure-diagnosis.json`，完整输入/输出清单见
  `50-final-evidence-manifest.json`；截至本轮停写的冻结清单为
  `82-final-evidence-manifest.json`。这些文件保留原始失败，不用后续重算覆盖。

### 首轮执行事实（未通过）

无历史上下文的 Codex 执行员从原始需求自行选型：27 件、128 个物理引脚，目标中
85 个连接、43 个明确 NC，分为 8 个有唯一归属的功能区和两张 A4 原理图。独立源审查
核对了 19 项专属附件和 7 个 `direct` 网的真实线树；这是**离线源审查**，不代表现场
通过。P1/P2 的 source、布局候选、固定渲染与受保护队列均有留档。

P1 的 189 步队列执行了前 187 步，第 188 步严格门禁失败，显式保存第 189 步未运行。
失败后新鲜 typed 回读确认 P1 有 17 件、58/58 个目标引脚连接、13 个网络，器件身份与
目标一致；`layout-lint`、分区和 `bridge-check` 通过。`sch check` 找到 R3、D5、J1、
C6 的位号与真实导线相交，以及图签 Name/Drawed/Description 未填；官方 DRC 另返回
`fatal=0,error=0,warn=2`，但公开 API 只给汇总，无法从该结果断言是哪两个对象。
P2 未 Apply，保存、真实重载及局部现场修改未执行。P1 处于部分写入状态，持久化未验证。

| 场景 | 首轮状态 | 依据 |
|---|---|---|
| M1 位号测量 | `not-run` | `dev.1` 无完整导出；新命令单元负例通过，仍待新连接器现场测量。 |
| F1 整体生成 | `fail` | P1 严格门禁未通过；P2 未落图。 |
| F2 落图与持久化 | `fail`（P1 部分落图）；P2/显式保存/真实重载 `not-run` | P1 的 `47` 图形差分为 `incomplete` 且 `drawingCompared=false`，不能凭 58/58 个引脚连接宣称对象完整对账。 |
| L1 核心局部移动 | `offline-only` | U2 在 P1 源上的 10 raw 移动可重算；UART 区随动、其他三区逐项不变；尚未现场 Apply。 |
| L2 单脚标记修复 | `not-run` | 尚无可回读的旧错误支路，不人为制造缺陷。 |
| N1 写前拒绝 | `offline-partial` | 跨页 8 区源、同数量异页目标（`77`）、文档 ID 漂移（`78`）、非 5 raw 网格、缺归属、`direct` 网无真实线树及缺精确 device UUID 均非零拒绝；同页陈旧现场状态仍待测。 |
| R1 重现与幂等 | `offline-partial` | 旧源重复 zone/sheet/compose 字节与哈希一致；最终含完整位号 bbox 的源和保存重载后幂等仍待测。 |

这轮发现的通用缺口是：布局前没有完整的真实 Designator bbox 导出；图签没有作为
逐页源字段进入 compose；官方原理图 DRC 只返回聚合警告。现有设计用 CH340C 加
BOOT/RESET 人工按键进入下载模式；原需求“插上 USB 就能烧录”是否要求自动下载仍
待判定，在明确前不把该功能条款记为通过。后续只可在保留当前 journal 和 fresh 快照后
从源重新计算，生成新的完整受保护队列；不能从失败步骤继续或现场逐件补丁。

独立电气复核将 P1 的 58 条 fresh 引脚连接逐项对上源目标：USB 及 5V 端子经两只
SS34 合到 `VIN_OR`，U3 的 FB 分压标称约 3.318V；这仅证明原理图连接和计算值，
尚无输入电流、二极管损耗、电容有效容量与热设计的闭环。P2 只有源目标：EN/BOOT
上拉和按键、TX/RX 对接、IO4 串联 330Ω LED 的方向成立，未有现场回读。U2 的
DTR#/RTS# 被明确设为 NC，因此若需求包含免按键自动下载，当前源目标不满足。
这些功能事实与布局、持久化验收分别判断；参考[独立电气核查证据](../../artifacts/easyeda-v160-schematic-20260923/46-page-1-partial-connectivity.json)。

### `dev.3` 刷新后复测（连接器运行态回退，已停写）

用户刷新后，`health` 一度确认 CLI、daemon、connector 均为 `1.6.0-dev.3`，目标工程/P1
和 Web 宿主正确。新上下文执行员 fresh typed 回读并逐对象比对：P1 仍是 17 个 part、
19 个 netport、10 个 netflag、1 张图纸，29 条 wire primitive；所有组件与导线 ID 和
完整内容均与首轮 `42` 快照一致。P2 只有图纸，与原空白 `30` 快照一致。旧 `82` 清单所列
235 个证据文件哈希全部重算匹配。本轮未写设计对象。

从 P2 切回 P1 的 typed `sch designator-geometry --doc` 中，`document.open` 超时；随后新
`windowId` 报告 connector `1.6.0-dev.2`，运行态不再与 CLI/daemon 同版。执行员立即停下
测量与所有现场写入；27/27 位号实测、完整重算、显式保存重载与局部现场移动仍未进行。
现场停写证据为忽略目录 `83`–`98`，其中 `93` 是停写摘要，`96` 是 daemon action 审计，
`97` 是离线版本漂移诊断，`98` 为新证据 SHA 清单。三个本地 `.eext` 包共享同一 UUID；
仅从本地包无法判断浏览器里实际安装项。需用户检查仅目标扩展启用，重开 Web 标签后再
核对运行版本与目标页；不能把曾短暂出现的 `dev.3` 作为后续动作通过证据。

### `dev.3` 重开后的位号测量与离线阻塞

用户重开 Web 标签后，`99` 的 `health` 与 `100` 的本地包检查再次确认 CLI、daemon、运行中的
connector 和已安装 Skill 均为 `1.6.0-dev.3`，目标工程和 P1 精确匹配。新上下文执行员
用 typed `sch designator-geometry` 测得 P1 的 17/17 个位号；P2 尚无器件，因此在专用临时
P3 页按源身份放置其 10 件，测得 10/10 个位号，随后按创建时返回的页 UUID 做 typed 删除。
全程未修改 P1/P2 的设计对象，删除前后两页组件与导线的完整 `result` 深度相等；
原始 daemon 审计中创建与删除请求均指向 P3 的同一 UUID。独立无上下文评审逐一复核 27 件
的唯一可见位号、parent/页、官方 bbox 和源绑定，以及 P3 删除审计链。此阶段的**测量与
非持久化清理**记 M1 `pass`；临时页删除后的 save→reload→fresh 持久化核查仍是
`incomplete`。证据为 `101`–`144`、独立审查记录 `173` 与冻结清单 `171`。

执行员将 27 个真实位号 bbox 合入严格源副本 `148`，其他源字段逐键保持不变；含额外
`measurement.textBBoxProvenance` 的先前副本被 schema 拒绝，失败证据 `146`–`147` 留存。
严格源的 BUCK 区在 20 万、40 万、100 万候选上界下均未找到合法布局，三档输入仅候选预算
不同（`152`、`155`、`158`）。报告均写明 `globalInfeasibilityProven:false`；因此这是
**有界搜索未完成**，不能说设计无解，也不能从旧无位号布局继续 Apply。旧布局中 C6 位号
确与 3V3 导线相交。独立求解器审查发现，非路由类终端错误会被“为重定位保留预算”覆盖，
但重定位仅能处理有完整 blocker 的路由冲突；三次均无定向重定位。这是通用求解器问题，
须保留真实冲突原因并修正预算使用，再从 `148` 重算。下游 sheet/render/compose、全图
Apply、显式保存、真实重载和局部现场修改均未执行，状态仍为 `blocked`/`not-run`。

本阶段成本已记 `172`：墙钟 16.97 分，daemon 0.52 分，96 次调用、0 失败；token 未记录。
NPN 器件当前站点的 UUID 与绑定仅作只读查询，B/E/C 到实际引脚/焊盘的映射尚未现场确认。
“插上 USB 就能烧录”暂按无需手按 BOOT/RESET 的较严格含义规划验收；现有 U2 的
DTR#/RTS# 明确 NC，故该条目前不能通过。该解释待用户纠正时再调整，不能把拟议电路当成
已落图或已验收事实。

| 场景 | `dev.3` 最新状态 | 第一条未满足的事实 |
|---|---|---|
| M1 位号测量 | `pass`；持久化清理 `incomplete` | 27/27 实测与独立复核完成；P3 删除尚无保存重载核查。 |
| F1 整体生成 | `blocked` | 带真实位号的 BUCK 区求解耗尽候选预算；无完整合法区内布局。 |
| F2 落图与持久化 | `not-run` | 新源尚无完整受保护队列。首轮 P1 的部分写入仍不算通过。 |
| E1 USB 烧录控制 | `fail`（严格解释） | U2 的 DTR#/RTS# 为明确 NC，尚无自动下载控制链。 |
| L1 核心局部移动 | `not-run` | 无带真实位号的合法整页基线。 |
| L2 单脚标记修复 | `not-run` | 未回读到满足修复前提的真实缺陷。 |
| N1 写前拒绝、R1 重现 | `offline-partial` | 已有负例和旧源重现不能覆盖新源的完整计划及保存重载。 |

### 自动下载候选与器件证据

为按严格解释核查 E1，新上下文执行员在独立临时页放置当前站点 S8050 `C20069125`。
新鲜 typed 实例回读证实符号脚 B=1、E=2、C=3，器件 UUID 与库查询一致；创建和删除
请求在 daemon 原始审计中指向同一临时页 UUID。删除后原 P1/P2 的页、器件、引脚和导线
即时回读完全相同。独立只读评审复算 25 项 SHA、原始审计行和对象差分后通过上述判据。
封装 pad 到 B/E/C 的映射没有 typed 回读，清理后的持久化也未保存重载，均为
`incomplete`；证据见 `174-npn-pin-probe/` 的原始回包和 `175-npn-independent-audit.json`。

新的离线源副本 `175-autodownload-source-candidate/candidate-premeasurement-incomplete.json`
保留原 27 件及其 bbox，仅将 U2 的 DTR#/RTS# 从 NC 接入控制网，并新增 Q1/Q2、R8/R9
作为 U2 的专属外围。首次静态审查漏掉 Q1/Q2 各有两条 attachment；补齐四件几何后的
typed `layout-plan --zones` 以 `invalid/duplicate attachment cmp-q1` 非零拒绝。独立评审
已将 `176-auto-source-independent-audit.json` 修正为 `fail`，旧 SHA 候选标
`candidate-rejected`，不得 Apply。其余旧件保全、真实符号脚号、跨区 EN/IO0 策略和四种
控制线状态的理想静态逻辑检查不覆盖此结构错误。静态原理图也不能证明实板免按键烧录。

`177-auto-four-measure/` 在专用临时页测得 Q1/Q2/R8/R9 的官方本体和位号 bbox，删除后
P1/P2 即时对象回读不变。独立审查 `179-four-measure-independent-audit.json` 复算器件、
引脚、几何、版本和原始创建/删除审计；仍明确保存重载及封装 pad 映射未覆盖。根据信息
生成新的 `180-auto-strict-measured/strict-measured-31-attachment-fixed.json`，只移除两条
重复的布局附件，保留所有电气引脚网络，将每区候选上界调至 40 万。当前源码的离线
`layout-plan --zones` 八区均 planned（合计 529746 候选），`layout-sheet-plan` 选出三张
A4 页；三页独立 `layout-render` 与固定 `compose` 通过。这里的 PNG 是简化布局预览，
不是官方编辑器截图或现场对象回读。

三页分配为 P1 USB/端子/降压，P2 CH340/自动下载，P3 主控/按键/LED。P3 已在专用
测试工程 typed 创建，UUID `b455f8efee65a626`，P1/P2 对象回读仅图框派生
`@Page Count` 从 2 变 3；用建页后的 fresh 快照重新编制三份受保护队列，175/65/95
步，安装态 `dev.3` 的三份 dry-run 均通过。队列尚未执行，显式保存、真实重载、局部
现场修改仍未开始。由于 U2/C4 从旧 P1 迁至 P2，P1 的完整器件集合变化，
`--preserve-instances` 按契约拒绝；当前队列使用有新鲜前置守卫的 `--replace`。
独立预执行审查 `183-preapply-independent-audit.json` 判定 **fail / mayExecute=false**：
P1 队列清页前仅核对 17 个器件，未守卫现场已有的 29 条 wire primitive（132 个线段）、
10 个 netflag 和 19 个 netport；紧接着的 `sch clear` 会删除它们。向现场添加一条器件数
不变的导线就能构成最小反例，三页 dry-run 成功不能证明清页安全。旧三份队列已冻结为
`candidate-rejected`，不得执行。须补齐清页前的完整图元清单守卫与负例，重新读取三页现场、
重编队列并交无上下文 Agent 复审。U2/C4 的旧 `uniqueId` 也会因跨页迁移而重建，实例
关联风险需在执行后单列核对。求解器的有界搜索及预算诊断修复已通过 `go test ./...`、
`make skill-check` 和离线 200k/400k 边界回归：200k 仍报告 MCU 有界失败，400k 的八区
计划全部成功且布局与前一补丁字节一致；冻结证据在 `179-budget-diagnostic/`。
这些回归不构成 `dev.4` 连接器的现场验收。

`dev.4` 的通用清页守卫已在离线代码复审中通过：`sch list --include-page-primitives`
采集元件、标记、导线、图形、属性和嵌入对象的原生状态；compose 的写前检查及 typed
`sch clear` 的首次删除前检查均使用它。普通清页遇到无法保证会删除的孤立属性或嵌入对象
会写前拒绝；保留实例清页对这两类对象变化、同 ID 导线/标记变化及缺 getter 均有零删除
负例。独立无上下文评审确认代码层原 B1 关闭，但**仅为离线通过**；目标宿主是否提供
完整 SDK 读取、三页新快照能否编译、现场 Apply 和持久化均须另验。全量 Go 3575 项、
连接器 460 项及 typecheck、Skill/Agent/release 输入检查通过。
