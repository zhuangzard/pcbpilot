# 里程碑档人工走查 — 脚本与记录(ADR-0002 验收项)

> 交互模式定义:`.agents/skills/pcbpilot/references/design-flow.md` →「交互模式(Interaction Modes)」;
> 决策依据:`.agents/skills/pcbpilot/references/design-decisions.md`;裁决:`docs/adr/0002-*.md`。
> 本文是**走查执行脚本 + 记录表**:用户扮演客户,验证里程碑确认模式的交互体验。每次走查在下方追加一节记录。

## 走查脚本(用户当客户,约 15–30 分钟)

1. **准备**:用户口头/文字给一个板子需求(可以就用 `esp32MiniRequire.md`,或现编一个新需求;**不要**给 BOM/网表/板框等加工过的答案)。
2. **确认点 ①(S0 方案书)**:agent 按里程碑档跑 S0,产出方案书摊给用户。用户重点体验:
   - 决策点是不是**按需求相关性过滤**过(无 USB 的板不该问 USB 取向)?
   - 一次呈现的决策数量是否可消化(目标:一屏内,每条有推荐默认)?
   - 「全部按推荐来」是不是低摩擦一句话就能过?
   - 有没有把 guardrail(唯一正确答案)误摊成选择题?
3. **确认点 ②(转 PCB 前)**:原理图完成后 agent 停住报告。体验:报告是否给了「网表全对/门禁全过」的可判断证据,而不是要用户自己去查。
4. **确认点 ③(交付前)**:P10 双门后 agent 停住。体验:交付摘要是否说清了偏差(如降级决策、遗留 WARN)。
5. **全程反例观察**:中途 agent 有没有在**不该问的地方问**(save/reload/DRC 这类 guardrail),或在**该停的里程碑不停**。

## 记录表模板

```
### 走查 #N — YYYY-MM-DD
- 需求输入:(一句话)
- 确认点①:呈现决策 X 条 / 相关性过滤 ✓|✗ / 可消化 ✓|✗ / 低摩擦默认 ✓|✗ / guardrail 误摊 有|无
- 确认点②:证据充分 ✓|✗ (缺什么)
- 确认点③:偏差说清 ✓|✗
- 反例:  (不该问而问 / 该停不停,各列出)
- 结论与改进项:(落到 design-flow.md / design-decisions.md 的具体修改)
```

## 记录

### 走查 #1 — 2026-07-08(进行中)
- 需求输入:esp32-barrier 道闸板——从真实固件工程提取需求,ESP32-S3 **芯片级** + CC1101 + SD + RS485(case:`showcase/001-esp32-barrier`)
- 确认点①:呈现决策 **4 条** / 相关性过滤 **✓**(芯片级引入 CC1101 形态、天线形式、现场供电等新决策;USB HUB、自动下载等无关/已定项未拿来问)/ 可消化 **✓**(一屏 4 问,每问 2-3 选项带坑)/ 低摩擦默认 **✓**(用户四项全按推荐,4 次点击过门)/ guardrail 误摊 **无**(天线全层禁布、单地域 PLANE、USB 单取向作为"已定项"列出而非问题)
- 亮点:需求来源是固件代码而非口述——引脚表、LED 极性(低电平点亮+15k)这类细节直接从固件注释提取,方案书可信度高
- 确认点②(2026-07-09):原理图三页完成停点。证据=EasyEDA 几何 check 三页 0 跨网混线/0 悬空线/备用脚白名单吻合;**诚实披露**:原生 netlist 引擎文档级死亡(上游缺陷),成员级网表验证移交 PCB 阶段(add-component 手喂 spec + DRC 连通把关)。体验观察:执行段(S1-S6)完全无人值守 ✓,但布线自动化在芯片级密度暴露 6+ 工具缺陷,修复消耗远超预期——走查的核心发现是**工具成熟度边界**而非交互设计问题。**用户抓包补充(2026-07-09)**:确认点②首版证据不充分——native DRC 未跑,用户 UI 一跑 6 致命(孤儿旗端点压 pin)。返工后 native DRC 0 fatal 过门+端点扫描三页 0。教训:确认点②的证据必须含 native DRC,check 不能替代(两引擎规则集不重叠)
- **布局阶段用户纠偏(2026-07-09,重要)**:P2 摆放缺确认点——agent 一口气盲摆 103 件,用户点出 6 条:双面布局要问、边缘接口件(开口方向)要按优先级+确认、丝印时机要确认、封装(贴片 vs 手焊)要 S0 问、螺孔净空没避开(机械验证:4 处压垫圈区)、每档布局完成要确认检查。已固化:design-flow P2 重写为**分档确认制**(孔→边缘接口→主芯片→卫星),design-decisions 新增 #13 双面布局、#14 焊接工艺
- 确认点③:待交付前(未到)
- 结论(阶段性):确认点①体验成立;待走完全程后回填 ②③ 与改进项
